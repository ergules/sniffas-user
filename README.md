signIn users, manage usersDb
authenticate over Firebase Auth and generate JWT tokens

### TO RUN LOCALLY

override port with --server.port
override properties with --spring.config.additional-location eg: optional:file:/opt/some_file